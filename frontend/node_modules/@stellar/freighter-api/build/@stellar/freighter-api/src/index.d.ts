import { getPublicKey } from "@stellar/freighter-api/build/@stellar/freighter-api/src/getPublicKey";
import { signTransaction } from "@stellar/freighter-api/build/@stellar/freighter-api/src/signTransaction";
import { signBlob } from "@stellar/freighter-api/build/@stellar/freighter-api/src/signBlob";
import { signAuthEntry } from "@stellar/freighter-api/build/@stellar/freighter-api/src/signAuthEntry";
import { isConnected } from "@stellar/freighter-api/build/@stellar/freighter-api/src/isConnected";
import { getNetwork } from "@stellar/freighter-api/build/@stellar/freighter-api/src/getNetwork";
import { getNetworkDetails } from "@stellar/freighter-api/build/@stellar/freighter-api/src/getNetworkDetails";
import { isAllowed } from "@stellar/freighter-api/build/@stellar/freighter-api/src/isAllowed";
import { setAllowed } from "@stellar/freighter-api/build/@stellar/freighter-api/src/setAllowed";
import { getUserInfo } from "@stellar/freighter-api/build/@stellar/freighter-api/src/getUserInfo";
import { requestAccess } from "@stellar/freighter-api/build/@stellar/freighter-api/src/requestAccess";
export declare const isBrowser: boolean;
export { getPublicKey, signTransaction, signBlob, signAuthEntry, isConnected, getNetwork, getNetworkDetails, isAllowed, setAllowed, getUserInfo, requestAccess, };
declare const _default: {
    getPublicKey: () => Promise<string>;
    signTransaction: (transactionXdr: string, opts?: {
        network?: string | undefined;
        networkPassphrase?: string | undefined;
        accountToSign?: string | undefined;
    } | undefined) => Promise<string>;
    signBlob: (blob: string, opts?: {
        accountToSign?: string | undefined;
    } | undefined) => Promise<string>;
    signAuthEntry: (entryXdr: string, opts?: {
        accountToSign?: string | undefined;
    } | undefined) => Promise<string>;
    isConnected: () => Promise<boolean>;
    getNetwork: () => Promise<string>;
    getNetworkDetails: () => Promise<{
        network: string;
        networkUrl: string;
        networkPassphrase: string;
        sorobanRpcUrl?: string | undefined;
    }>;
    isAllowed: () => Promise<boolean>;
    setAllowed: () => Promise<boolean>;
    getUserInfo: () => Promise<{
        publicKey: string;
    }>;
    requestAccess: () => Promise<string>;
};
export default _default;
